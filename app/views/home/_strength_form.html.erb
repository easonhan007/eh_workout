<%= turbo_frame_tag 'strength' do%>
    <%= form_with(model: record) do |f| %>
        <div class="row mb-2">
            <div class="col-auto">
                <%= f.label(:weight, class: 'col-form-label') %>
            </div>
            <div class="col-auto">
                <%= f.number_field(:weight, class: 'form-control', min: 1)%>
            </div>
            <div class="col-auto">
                <%= f.label(:reps, class: 'col-form-label') %>
            </div>
            <div class="col-auto">
                <%= f.number_field(:reps, class: 'form-control', min: 1)%>
            </div>
        </div>
    <% end %>
<% end %>