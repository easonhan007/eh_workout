<turbo-frame id="reps_only">
    <%= form_with(model: record) do |f| %>
        <div class="row mb-2">
            <div class="col-auto">
                <%= f.label(:reps, class: 'col-form-label') %>
            </div>
            <div class="col-auto">
                <%= f.number_field(:reps, class: 'form-control', min: 1)%>
            </div>
            <div class="col-auto">
                <%= f.submit(class: 'btn btn-outline-success', value: 'Save')%>
            </div>
            <%= render(partial: 'home/flag', locals: { msg: msg })%>
            <%= f.hidden_field(:event_id) %>
            <%= f.hidden_field(:exercise_id) %>
        </div>
    <% end %>
    <%= render(partial: 'home/errors', locals: {record: record})%>
</turbo-frame>